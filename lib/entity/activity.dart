class ActivityInfo {
  final String imgUrl;
  final String title;
  final String description;
  final int status;
  final String startDate;
  final String endDate;

  const ActivityInfo(this.imgUrl, this.title, this.description, this.status,
      this.startDate, this.endDate);
}
